[build]
  base = "client"
  command = "npm run build"
  publish = "dist"

# React Router uses client-side routing — without this, refreshing on
# /order, /admin, etc. 404s because Netlify looks for a matching file.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
